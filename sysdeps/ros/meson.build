sysdep_supported_options = {
	'posix': false,
	'linux': false,
	'glibc': false,
	'bsd': false,
}

rtld_sources += files(
)

libc_sources += files(

)

if not no_headers
	install_headers(
		follow_symlinks: true
	)
endif
